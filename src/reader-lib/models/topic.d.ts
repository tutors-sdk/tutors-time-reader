import type { Lo } from "../types/lo-types";
export declare class Topic {
    lo: Lo;
    units: Lo[];
    panelVideos: Lo[];
    panelTalks: Lo[];
    standardLos: Lo[];
    toc: Lo[];
    constructor(lo: Lo, courseUrl: string);
}
